# GH-527: Get to File from Task Details
**Status:** PENDING_APPROVAL
**Issue:** https://github.com/ntm-strategic-software/StagesApp-mobile/issues/527
**Branch:** gh527
**Created:** 2026-04-15

## Issue Summary
From a "Review with Helper" task in the Task Details screen, the user wants a way to jump directly to the associated CLA file (Review Culture Event screen) and return, instead of navigating back through multiple screens.

## Analysis
**This issue appears to already be resolved.** A collaborator comment from 2025-12-18 states: "I think this is already handled by #473 just need to verify the current version is doing what we want for this issue."

The current `task-details.tsx` (line 65) already has a "Play File" button (`PlayFileButton` component) that navigates directly to `routes.REVIEW_CULTURE_EVENT` with the culture event ID. React Navigation's stack provides a built-in back button to return.

## Implementation Plan
1. **Verify existing functionality**: Test the "Play File" button on the Task Details screen for a "Review with Helper" task to confirm it navigates to the file and that the back button returns correctly.
2. If working as expected, close the issue with a comment confirming it's resolved.
3. If not working, investigate what's missing (the navigation code at lines 60-69 of task-details.tsx looks correct).

## Files to Modify
- Likely none — needs verification only

## Testing Strategy
- Navigate to a "Review with Helper" task in the Listen and Do taskbox
- Open Task Details
- Tap "Play File" button
- Verify it opens the correct CLA file in the Review Culture Event screen
- Tap back and verify return to Task Details