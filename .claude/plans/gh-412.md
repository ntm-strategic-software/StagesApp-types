# GH-412: In a Taskbox, would be nice to see what Activity the AP is created from
**Status:** PENDING_APPROVAL
**Issue:** https://github.com/ntm-strategic-software/StagesApp-mobile/issues/412
**Branch:** gh412
**Created:** 2026-04-15

## Issue Summary
Show the Activity name that an Activity Plan (AP) was created from, both in the Taskbox view and in the Task view. A recent comment says "This may be done - check when the AP has no title, and when it has a title."

## Analysis
**This issue appears to already be implemented.** In `src/components/shared/daily-tasks.tsx`:
- Line 7: Imports `getActivityDisplayName` from `stagesapp-types`
- Line 44: Gets `activityDisplayName` using `getActivityDisplayName(activityPlan.activityKey, localize.locale())`
- Line 42: Gets `planTitle` from `activityPlan.activityPlanTitle`
- Line 52: When there's no title, shows `[untitled {activityDisplayName}]` as fallback

The activity name is already displayed as a fallback when the AP has no title.

## Implementation Plan
1. **Verify existing behavior**: Test with APs that have titles and APs without titles
   - When AP has a title: verify it shows the title (check if activity name is also shown)
   - When AP has no title: verify it shows `[untitled {ActivityName}]` format
2. Per the issue comment, if both scenarios look good, close the issue
3. If the activity name should also show when the AP *does* have a title (not just as fallback), add it as a subtitle or parenthetical

## Files to Modify
- Likely none — needs verification only
- If activity name should always show: `src/components/shared/daily-tasks.tsx` (minor text change)

## Testing Strategy
- View a Taskbox with APs that have titles — verify display looks correct
- View a Taskbox with APs that have no titles — verify activity name fallback shows
- Check both the Taskbox list view and the individual Task Details view